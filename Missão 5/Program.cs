// Missão 5

// (F) No pior caso, o algoritmo Insertion Sort é de complexidade O(n²)

// (V) O algoritmo Insertion Sort é eficiente para listas com poucos elementos, mas se torna muito lento em listas grandes.

// (V) O algoritmo Insertion Sort funciona ordenando um elemento por vez, comparando-os com os elementos e movendo-o para a posição correta.

// (V) O algoritmo Insertion Sort é in-place, ou seja, ele ordena o vetor original sem a necessidade de criar um novo vetor.

